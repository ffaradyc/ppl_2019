import 'dart:io';

void main() {
  //stdout.write('Nama Lengkap: ');
  // print('Nama Lengkap: ');
  // var nama = stdin.readLineSync();
  // nama == 'Budi' ? print('Selamat Pagi, $nama') : print('Selamat datang, $nama');

  // int a = 8, b = 3;
  // int total = a / b;
  // print('$a / $b = $total');

  List<String> angka = ['satu', 'dua', 'tiga', 'empat'];

  for (var item in angka)
  {
    print('Ini adalah angka $item');
  }
}
